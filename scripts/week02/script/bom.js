// bom.js - Basic DOM setup for Book of Mormon favorite chapters app

// Declare variables that hold references to the input, button, and list elements
const input = document.querySelector('#favchap');
const button = document.querySelector('#submit-btn');
const list = document.querySelector('#list');

// The delete button is NOT functional - it's just for visual display
// Functionality will be added in the next activity

console.log('App initialized with static example');